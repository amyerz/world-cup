// Root build file. Versions are pinned to what is already cached on this machine
// (Gradle 9.4.1 / AGP 9.2.1) so a clean build needs no surprise downloads.
plugins {
    id("com.android.application") version "9.2.1" apply false
}
